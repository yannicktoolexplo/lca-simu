# Geocoding

Ce dossier contient le geocodage hors ligne du graphe supply.

- `geocode_nodes_offline.py` : script de geocodage deterministe.
- `legacy_actor_coords.json` : table de coordonnees connue pour les acteurs.

Les sorties vont dans `etudecas/data/geocoded`.
