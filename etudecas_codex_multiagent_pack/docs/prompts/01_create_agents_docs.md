# Prompt Codex - Maintenir les roles agents

Tu maintiens le pack `etudecas_codex_multiagent_pack`.

Objectif : garder les roles agents alignes avec le vrai repo Etudecas.

A faire :

- verifier `AGENTS.md` ;
- verifier `docs/agents/*.md` ;
- verifier `etudecas_agentkit/agents/roles.py` ;
- supprimer les consignes obsoletes ou les marquer comme references ;
- eviter toute collision avec le vrai package `etudecas`.

Contraintes :

- ne pas copier le mini-kit dans le vrai repo ;
- ne pas introduire de gros artefact ;
- garder les roles actionnables avec entrees, livrables et criteres de done.
